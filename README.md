📘 Thymeleaf Examples – README (Developer Style)Spring Boot와 Thymeleaf 기본 문법을 체계적으로 학습하기 위한 핵심 예제 모음입니다. 변수 표현식부터 반복문, 조건문, URL 처리, 그리고 조각 페이지(Fragment)까지, 타임리프의 핵심 기능을 파일 단위로 정리했습니다.📂 프로젝트 구조Plaintexttemplates/
├── m1.html # 변수 표현식
├── m2.html # 메시지(i18n)
├── m3.html # 연산자
├── m4.html # HTML 속성 조작
├── m5.html # 콘텐츠(text/utext/inline)
├── m6.html # 숫자/날짜 포맷
├── m7.html # 링크 표현식
├── m8.html # 조건문/반복문/status
├── m9.html # 조각 페이지(fragment)
└── inc/
    ├── sub1.html
    └── sub2.html
📄 예제 상세파일주제핵심 내용m1Variable / Selection Expression${} 변수 표현식 (DTO/Map 출력), th:object + *{} 선택 변수 표현식m2Spring Messages (i18n)messages.properties 문자열 관리, 다국어 처리, 메시지 포맷팅m3Operators산술/비교/논리/삼항 연산자, 문자열 결합, 파이프 (`m4HTML Attribute Manipulationth:value, th:checked, th:selected, th:style, HTML 속성 우선순위 이해m5Text / UText / Inlineth:text vs th:utext, 인라인 표현식 ([[...]], [(...)]), th:inline="javascript"로 JS에 서버 값 전달m6Number & Date Formatting#numbers.formatInteger, formatDecimal, #dates.format, 날짜 요소 추출 (year, monthName 등)m7URL Expression@{} URL 빌드, 쿼리 파라미터/PathVariable 바인딩, 문자열 방식과 비교m8Conditional / Loop / Statusth:if, th:unless, th:switch, th:each 반복문, 상태값 (index, count, size, even/odd), th:block 활용, 주소록 테이블 렌더링m9Fragment (include)th:fragment 정의, th:insert vs th:replace, Fragment 파라미터 전달▶ 실행 방법Spring Boot 프로젝트 기준으로 다음 URL로 접근하여 각 예제를 확인합니다.http://localhost:8080/m1
http://localhost:8080/m2
...
http://localhost:8080/m9
📌 각 URL은 TestController에서 매핑한 Thymeleaf 페이지에 대응합니다.📝 Today I Learned (TIL)핵심 기능을 학습하며 정리한 내용입니다.타임리프는 HTML 속성 기반 템플릿 엔진이다.th:* 속성이 핵심이며, JSP의 태그 중심 방식과 구조적으로 다릅니다.출력 방식의 차이를 정확히 이해했다.th:text = Escape 처리 (<를 &lt; 등으로 변환)th:utext = Unescaped 처리 (HTML 태그 그대로 출력)[[...]] = 인라인 Escape[(...)] = 인라인 UnescapedURL 빌딩은 @{}가 가장 안전하다.파라미터 자동 인코딩 및 복잡한 문자열 연결을 제거합니다.PathVariable 처리 방식이 간단합니다.반복문 + status 조합이 실무에서 유용하다.인덱스, 행 번호, 홀/짝 구분 가능 (${status.index}, ${status.odd})조건부 스타일 적용에 매우 적합합니다.Fragment로 레이아웃 설계가 가능해졌다.반복되는 UI 요소 (헤더, 푸터 등)를 분리하여 재사용할 수 있습니다.타임리프의 Include 구조를 완벽히 이해했습니다.JSP 대비 장점 정리HTML 친화적: 디자이너도 템플릿을 쉽게 이해하고 수정할 수 있습니다.유지보수 용이: 순수 HTML 형태를 유지합니다.프론트 템플릿 구조와 유사: Vue/React 컴포넌트 구조 이해에 도움을 줍니다.스프링 MVC와 결합도가 높습니다.✔ Summary이 저장소는 타임리프의 주요 기능을 빠르게 복습하고, Spring MVC 프로젝트에서 바로 적용할 수 있는 코드 패턴을 정리하는 것을 목표로 합니다.
